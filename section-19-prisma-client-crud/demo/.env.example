# PostgreSQL Docker Connection (docker-compose -f docker-compose.postgres.yml up -d)
# User: postgres | Password: postgres123 | Database: course_db
DATABASE_URL="postgresql://postgres:postgres123@localhost:5432/course_db?schema=public"
