# Section 31: MongoDB Fundamentals

## Topics Covered

### 1. What is MongoDB & NoSQL
- Document-oriented database concepts
- NoSQL database categories
- MongoDB architecture overview
- When to use NoSQL vs SQL

### 2. MongoDB vs SQL (Quick Comparison)
- Tables vs Collections
- Rows vs Documents
- Columns vs Fields
- Joins vs Embedded Documents
- Schema flexibility

### 3. Documents, Collections & BSON
- JSON vs BSON format
- Document structure and _id field
- Collection organization
- Database hierarchy

### 4. MongoDB Atlas Overview
- Cloud-hosted MongoDB
- Creating a free cluster
- Network access and IP whitelisting
- Database users and authentication

### 5. Using MongoDB Compass
- GUI for MongoDB
- Connecting to databases
- Viewing and editing documents
- Running queries visually

### 6. Basic CRUD Operations
- `insertOne()` and `insertMany()`
- `find()` and `findOne()`
- `updateOne()` and `updateMany()`
- `deleteOne()` and `deleteMany()`
- Query operators ($eq, $gt, $lt, $in, etc.)

## Demo Project
```bash
cd section-31-mongodb-fundamentals/demo
cp .env.example .env
pnpm install
pnpm dev
```
