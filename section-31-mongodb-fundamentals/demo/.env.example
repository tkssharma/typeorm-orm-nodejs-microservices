# MongoDB Connection (Docker)
# Start MongoDB: docker-compose -f docker-compose.mongodb.yml up -d
MONGODB_URI=mongodb://localhost:27017/mongodb_fundamentals

# With authentication (if needed)
# MONGODB_URI=mongodb://admin:password123@localhost:27017/mongodb_fundamentals?authSource=admin
